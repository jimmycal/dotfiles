# macOS noise
.DS_Store
# Node/pip caches if they sneak in
node_modules/
__pycache__/
*.pyc

# Don't accidentally commit secrets
secrets/
*.pem
*.key
*.crt
*.p12
.oci/config
.aws/
.kube/config*
*.env
.envrc.local

# Machine-local overrides (sourced by .zshrc but never committed)
*.local
